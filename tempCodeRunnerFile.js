applyDiscount = () => {
    if (checkOut["Milk"] >= 2) {
        milkTotal = "discounted version"
    }
    if(checkOut['Breadk'] >= 3) {
        breadTotal = "discounted version"
    }
    return "results"
}